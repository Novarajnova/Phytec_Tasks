//44.draw a flow chart to print Hexa value of given decimal no.

#include <stdio.h>
int main() {
    int n;
    printf("Enter decimal number: ");
    scanf("%d", &n);
    printf("Hex = %X\n", n);
    return 0;
}

